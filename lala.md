
**已检索仓库** *Perform detailed code review of the backend codebase focusing on security, code quality, and potential vulnerabilities* 0 个结果

**已查看目录** 23 个结果

**已查看文件** *authController.js*

**已查看文件** *authService.js*

**已查看文件** *userModel.js*

**已查看文件** *refreshTokenModel.js*

**已查看文件** *authMiddleware.js*


## Глубокий Code Review Backend-кода

### 1. `controllers/authController.js` - Регистрация родителя

#### Метод: `registerParent`
**Входные данные:**
- `req.body` с полями `email`, `password`, `name`

**Что делает под капотом:**
1. Проверяет наличие `req.body` (строка 7)
2. Деструктурирует данные из `req.body` (строка 9)
3. Проверяет, что все необходимые поля присутствуют (строка 10)
4. Вызывает `AuthService.registerParent` для регистрации пользователя (строка 13)
5. Устанавливает `refreshToken` в cookie (строки 14-19)
6. Возвращает ответ клиенту (строка 20)

**Возвращаемое значение:**
- JSON с полями `user` и `token` при успешной регистрации
- Ошибки с соответствующими HTTP-кодами при неудаче

**Взаимодействие с БД:**
1. Вызывает `AuthService.registerParent`, который:
    - Выполняет `UserModel.findByEmail(email)` - SQL-запрос для проверки существования email
    - Выполняет `bcrypt.hash(password, saltRounds)` - хеширует пароль
    - Выполняет `UserModel.createParent()` - SQL-запрос вставки нового пользователя
    - Выполняет `RefreshTokenModel.create()` - SQL-запрос вставки refresh токена

**Уязвимости:**
1. **Потенциальная утечка информации** (строка 22):
   ```javascript
   if (existing) throw new Error('EMAIL_EXISTS');
   ```

   Эта проверка позволяет злоумышленнику определить, существует ли email в системе.

2. **Недостаточная валидация email**:
   Нет проверки формата email перед передачей в сервис.

3. **Отсутствие rate limiting**:
   Нет защиты от брутфорса или массовой регистрации.

### 2. `services/authService.js` - Регистрация родителя

#### Метод: `registerParent`
**Входные данные:**
- `email`, `password`, `name`

**Что делает под капотом:**
1. Выполняет поиск пользователя по email (строка 24)
2. Если пользователь существует, выбрасывает ошибку 'EMAIL_EXISTS' (строка 25)
3. Хеширует пароль с помощью bcrypt (строки 28-29)
4. Создает нового родителя через `UserModel.createParent()` (строка 31)
5. Генерирует JWT и refresh токен (строка 32)
6. Возвращает объект с пользователем, access токеном и refresh токеном

**Возвращаемое значение:**
- Объект с полями `user`, `token` и `refreshToken`

**Взаимодействие с БД:**
1. `UserModel.findByEmail(email)` - SQL-запрос для проверки существования email
2. `UserModel.createParent()` - SQL-запрос вставки нового пользователя
3. `RefreshTokenModel.create()` - SQL-запрос вставки refresh токена

**Уязвимости:**
1. **Хранение хеша пароля в переменной**:
   Пароль хранится в переменной в открытом виде до хеширования.

2. **Отсутствие ограничений на сложность пароля**:
   Нет проверки сложности пароля перед хешированием.

### 3. `models/userModel.js` - Создание родителя

#### Метод: `createParent`
**Входные данные:**
- Объект с полями `email`, `password_hash`, `name`

**Что делает под капотом:**
1. Выполняет SQL-запрос вставки нового пользователя в таблицу `users`
2. Возвращает только безопасные поля пользователя

**Возвращаемое значение:**
- Объект с полями `id`, `email`, `role`, `name`

**Взаимодействие с БД:**
```javascript
prisma.users.create({
  data: {
    email: email,
    password_hash: password_hash,
    role: 'parent',
    name: name
  }
})
```


**Уязвимости:**
1. **Хранение хеша пароля в БД**:
   Использование поля `password_hash` вместо `password` может ввести в заблуждение других разработчиков.

2. **Нет ограничений на длину имени**:
   Поле `name` принимает любую строку без ограничений.

### 4. `controllers/authController.js` - Вход родителя

#### Метод: `loginParent`
**Входные данные:**
- `req.body` с полями `email`, `password`

**Что делает под капотом:**
1. Проверяет наличие `req.body` (строка 31)
2. Деструктурирует данные из `req.body` (строка 33)
3. Проверяет, что все необходимые поля присутствуют (строка 34)
4. Вызывает `AuthService.loginParent` для проверки учетных данных (строка 37)
5. Устанавливает `refreshToken` в cookie (строки 38-42)
6. Возвращает ответ клиенту (строка 43)

**Возвращаемое значение:**
- JSON с полями `user` и `token` при успешном входе
- Ошибки с соответствующими HTTP-кодами при неудаче

**Взаимодействие с БД:**
1. Вызывает `AuthService.loginParent`, который:
    - Выполняет `UserModel.findByEmail(email)` - SQL-запрос для поиска пользователя
    - Выполняет `bcrypt.compare(password, user.password_hash)` - проверка пароля
    - Выполняет `RefreshTokenModel.create()` - SQL-запрос вставки refresh токена

**Уязвимости:**
1. **Потенциальная утечка информации**:
   Ошибки "Invalid credentials" позволяют определить, существует ли email в системе.

2. **Нет защиты от брутфорса**:
   Не ограничено количество попыток входа.

### 5. `services/authService.js` - Вход родителя

#### Метод: `loginParent`
**Входные данные:**
- `email`, `password`

**Что делает под капотом:**
1. Выполняет поиск пользователя по email (строка 44)
2. Проверяет, что пользователь существует и является родителем или администратором (строки 45-46)
3. Проверяет наличие хеша пароля (строки 48-50)
4. Сравнивает хеш пароля с введенным паролем (строки 51-53)
5. Генерирует JWT и refresh токен (строка 55)
6. Удаляет хеш пароля из объекта пользователя (строка 56)
7. Возвращает объект с пользователем, access токеном и refresh токеном

**Возвращаемое значение:**
- Объект с полями `user`, `token` и `refreshToken`

**Взаимодействие с БД:**
1. `UserModel.findByEmail(email)` - SQL-запрос для поиска пользователя
2. `RefreshTokenModel.create()` - SQL-запрос вставки refresh токена

**Уязвимости:**
1. **Потенциальная утечка информации**:
   Ошибка "INVALID_CREDENTIALS" может помочь злоумышленнику определить, существует ли email.

2. **Нет ограничений на частоту попыток входа**:
   Не реализована защита от брутфорса.

### 6. `controllers/authController.js` - Создание ребенка

#### Метод: `createChild`
**Входные данные:**
- `req.body` с полями `name`, `pin`
- `req.userId` из токена

**Что делает под капотом:**
1. Проверяет наличие `req.body` (строка 81)
2. Деструктурирует данные из `req.body` (строка 83)
3. Проверяет, что все необходимые поля присутствуют (строка 84)
4. Вызывает `AuthService.createChild` для создания ребенка (строка 88)
5. Возвращает созданного ребенка клиенту (строка 89)

**Возвращаемое значение:**
- Объект с данными созданного ребенка
- Ошибки с соответствующими HTTP-кодами при неудаче

**Взаимодействие с БД:**
1. Вызывает `AuthService.createChild`, который:
    - Выполняет `bcrypt.hash(pin, saltRounds)` - хеширует пин-код
    - Выполняет `UserModel.createChild()` - SQL-запрос вставки нового ребенка

**Уязвимости:**
1. **Ограниченная проверка прав доступа**:
   Проверка принадлежности родителя к пользователю, отправившему запрос, делегирована сервису, но не реализована на уровне контроллера.

2. **Отсутствие ограничений на длину пин-кода**:
   Нет проверки минимальной длины пин-кода перед хешированием.

### 7. `services/authService.js` - Создание ребенка

#### Метод: `createChild`
**Входные данные:**
- Объект с полями `parentId`, `name`, `pin`

**Что делает под капотом:**
1. Хеширует пин-код с помощью bcrypt (строки 37-38)
2. Создает нового ребенка через `UserModel.createChild()` (строка 39)
3. Возвращает созданного ребенка

**Возвращаемое значение:**
- Объект с данными созданного ребенка

**Взаимодействие с БД:**
1. `UserModel.createChild()` - SQL-запрос вставки нового ребенка

**Уязвимости:**
1. **Нет проверки длины пин-кода**:
   Пин-код может быть слишком коротким, что делает его уязвимым для взлома.

2. **Отсутствие проверки уникальности пин-кода**:
   Нет гарантии, что пин-код уникален для ребенка.

### 8. `models/userModel.js` - Создание ребенка

#### Метод: `createChild`
**Входные данные:**
- Объект с полями `parentId`, `name`, `pinHash`

**Что делает под капотом:**
1. Выполняет SQL-запрос вставки нового пользователя в таблицу `users` с ролью 'child'
2. Возвращает только безопасные поля ребенка

**Возвращаемое значение:**
- Объект с полями `id`, `name`, `role`, `parentId`

**Взаимодействие с БД:**
```javascript
prisma.users.create({
  data: {
    parent_id: parentId,
    role: 'child',
    name: name,
    pin: pinHash
  }
})
```


**Уязвимости:**
1. **Хранение пин-кода в БД**:
   Использование поля `pin` вместо `pin_hash` может ввести в заблуждение других разработчиков.

2. **Нет ограничений на длину имени**:
   Поле `name` принимает любую строку без ограничений.

### 9. `controllers/authController.js` - Вход ребенка

#### Метод: `loginChild`
**Входные данные:**
- `req.body` с полями `childId`, `pin`

**Что делает под капотом:**
1. Проверяет наличие `req.body` (строка 97)
2. Деструктурирует данные из `req.body` (строка 99)
3. Проверяет, что все необходимые поля присутствуют (строка 100)
4. Вызывает `AuthService.loginChild` для проверки учетных данных (строка 102)
5. Возвращает ответ клиенту (строка 103)

**Возвращаемое значение:**
- Объект с полями `user` и `token` при успешном входе
- Ошибки с соответствующими HTTP-кодами при неудаче

**Взаимодействие с БД:**
1. Вызывает `AuthService.loginChild`, который:
    - Выполняет `UserModel.findById(childId)` - SQL-запрос для поиска ребенка
    - Выполняет `bcrypt.compare(pin, storedHash)` - проверка пин-кода

**Уязвимости:**
1. **Потенциальная утечка информации**:
   Ошибки "Invalid PIN" могут помочь злоумышленнику определить, существует ли ребенок.

2. **Нет защиты от брутфорса**:
   Не ограничено количество попыток ввода пин-кода.

### 10. `services/authService.js` - Вход ребенка

#### Метод: `loginChild`
**Входные данные:**
- `childId`, `pin`

**Что делает под капотом:**
1. Выполняет поиск ребенка по ID (строка 61)
2. Получает хеш пин-кода из объекта ребенка (строка 62)
3. Проверяет, что ребенок существует и имеет хеш пин-кода (строки 64-66)
4. Сравнивает хеш пин-кода с введенным пином (строки 68-69)
5. Генерирует JWT и refresh токен (строка 71)
6. Удаляет хеш пин-кода из объекта ребенка (строка 72)
7. Возвращает объект с ребенком, access токеном и refresh токеном

**Возвращаемое значение:**
- Объект с полями `user` и `token`

**Взаимодействие с БД:**
1. `UserModel.findById(childId)` - SQL-запрос для поиска ребенка
2. `RefreshTokenModel.create()` - SQL-запрос вставки refresh токена

**Уязвимости:**
1. **Потенциальная утечка информации**:
   Ошибка "INVALID_CREDENTIALS" может помочь злоумышленнику определить, существует ли ребенок.

2. **Нет ограничений на частоту попыток ввода пин-кода**:
   Не реализована защита от брутфорса пин-кода.

### 11. `controllers/authController.js` - Обновление токена

#### Метод: `refreshToken`
**Входные данные:**
- `refreshToken` из cookie

**Что делает под капотом:**
1. Получает refresh токен из cookie (строка 54)
2. Проверяет наличие refresh токена (строка 55)
3. Вызывает `AuthService.refreshAccessToken` для обновления токена (строка 56)
4. Возвращает новый access токен клиенту (строка 57)

**Возвращаемое значение:**
- Объект с новым access токеном
- Ошибки с соответствующими HTTP-кодами при неудаче

**Взаимодействие с БД:**
1. Вызывает `AuthService.refreshAccessToken`, который:
    - Выполняет `RefreshTokenModel.findByToken(refreshToken)` - SQL-запрос для поиска refresh токена
    - Выполняет `UserModel.findById(record.user_id)` - SQL-запрос для поиска пользователя
    - Выполняет `RefreshTokenModel.revoke(refreshToken)` - SQL-запрос удаления истекшего refresh токена

**Уязвимости:**
1. **Отсутствие защиты от replay-атак**:
   Нет проверки, что refresh токен используется только один раз.

2. **Нет ограничений на количество refresh токенов на пользователя**:
   Пользователь может иметь неограниченное количество активных refresh токенов.

### 12. `services/authService.js` - Обновление токена

#### Метод: `refreshAccessToken`
**Входные данные:**
- `refreshToken`

**Что делает под капотом:**
1. Находит refresh токен в БД (строка 76)
2. Проверяет, что refresh токен существует (строка 77-78)
3. Проверяет, что refresh токен не истек (строки 79-81)
4. Находит пользователя по ID из refresh токена (строка 83)
5. Генерирует новый access токен (строка 85)
6. Возвращает объект с новым access токеном

**Возвращаемое значение:**
- Объект с новым access токеном

**Взаимодействие с БД:**
1. `RefreshTokenModel.findByToken(refreshToken)` - SQL-запрос для поиска refresh токена
2. `UserModel.findById(record.user_id)` - SQL-запрос для поиска пользователя
3. `RefreshTokenModel.revoke(refreshToken)` - SQL-запрос удаления истекшего refresh токена

**Уязвимости:**
1. **Отсутствие защиты от replay-атак**:
   Нет проверки, что refresh токен используется только один раз.

2. **Нет ограничений на количество refresh токенов на пользователя**:
   Пользователь может иметь неограниченное количество активных refresh токенов.

### 13. `controllers/authController.js` - Выход из системы

#### Метод: `logout`
**Входные данные:**
- `refreshToken` из cookie

**Что делает под капотом:**
1. Получает refresh токен из cookie (строка 68)
2. Проверяет наличие refresh токена (строка 69)
3. Вызывает `AuthService.revokeRefreshToken` для отзыва refresh токена (строка 70)
4. Очищает cookie с refresh токеном (строка 72)
5. Возвращает сообщение об успешном выходе (строка 73)

**Возвращаемое значение:**
- JSON с сообщением "Logged out"
- Ошибки с соответствующими HTTP-кодами при неудаче

**Взаимодействие с БД:**
1. Вызывает `AuthService.revokeRefreshToken`, который:
    - Выполняет `RefreshTokenModel.revoke(refreshToken)` - SQL-запрос удаления refresh токена

**Уязвимости:**
1. **Нет проверки соответствия refresh токена пользователю**:
   Не проверяется, что refresh токен принадлежит текущему пользователю.

2. **Нет механизма отзыва всех refresh токенов**:
   Пользователь не может выйти со всех устройств одновременно.

### 14. `services/authService.js` - Отзыв refresh токена

#### Метод: `revokeRefreshToken`
**Входные данные:**
- `refreshToken`

**Что делает под капотом:**
1. Выполняет SQL-запрос удаления refresh токена из БД

**Возвращаемое значение:**
- Результат удаления refresh токена

**Взаимодействие с БД:**
```javascript
RefreshTokenModel.revoke(refreshToken) - SQL-запрос удаления refresh токена
```


**Уязвимости:**
1. **Нет проверки соответствия refresh токена пользователю**:
   Не проверяется, что refresh токен принадлежит текущему пользователю.

2. **Нет механизма отзыва всех refresh токенов**:
   Пользователь не может выйти со всех устройств одновременно.

### 15. `models/refreshTokenModel.js` - Модель refresh токена

#### Методы:
1. **create** - Создает новый refresh токен в БД
2. **findByToken** - Находит refresh токен по значению
3. **revoke** - Удаляет refresh токен

**Взаимодействие с БД:**
```javascript
// Создание refresh токена
prisma.refresh_tokens.create({
  data: {
    user_id: userId,
    token: token,
    expires_at: expiresAt
  }
});

// Поиск refresh токена
prisma.refresh_tokens.findUnique({
  where: { token: token }
});

// Удаление refresh токена
prisma.refresh_tokens.delete({
  where: { token: token }
});
```


**Уязвимости:**
1. **Нет механизма ограничения количества refresh токенов на пользователя**:
   Пользователь может иметь неограниченное количество активных refresh токенов.

2. **Нет автоматической очистки истекших refresh токенов**:
   Истекшие refresh токены остаются в БД и требуют ручной очистки.

### 16. `middleware/authMiddleware.js` - Аутентификация

#### Метод: `authenticate`
**Входные данные:**
- `Authorization` заголовок с Bearer токеном

**Что делает под капотом:**
1. Проверяет наличие и формат заголовка Authorization (строки 7-9)
2. Извлекает токен из заголовка (строка 11)
3. Проверяет и декодирует токен (строка 13)
4. Находит пользователя по ID из токена (строка 14)
5. Устанавливает данные пользователя в запрос (строки 17-19)
6. Вызывает следующий middleware (строка 20)

**Возвращаемое значение:**
- Устанавливает `req.user`, `req.userId`, `req.userRole`
- Возвращает ошибку 401 при неудаче

**Взаимодействие с БД:**
1. `UserModel.findById(decoded.userId)` - SQL-запрос для поиска пользователя по ID

**Уязвимости:**
1. **Нет проверки "свежести" токена**:
   Токен может быть очень старым, но все еще действительным.

2. **Нет проверки соответствия токена refresh токену**:
   Токен может быть выдан до отзыва refresh токена, но все еще действителен.

### 17. `middleware/authMiddleware.js` - Авторизация доступа к ребенку

#### Метод: `authorizeChildAccess`
**Входные данные:**
- `req.userId` из токена
- `req.userRole` из токена
- `req.params.childId` из URL

**Что делает под капотом:**
1. Получает ID ребенка из URL (строка 36)
2. Если нет ID ребенка, продолжает выполнение (строка 37)
3. Получает ID пользователя из токена (строка 39)
4. Получает ID ребенка из URL (строка 40)
5. Для роли 'child' проверяет совпадение ID (строки 45-48)
6. Для роли 'parent' проверяет принадлежность ребенка (строки 54-58)
7. Для роли 'admin' разрешает доступ (строки 63-64)
8. Возвращает ошибку 403 при неудаче

**Возвращаемое значение:**
- Продолжает выполнение middleware при успешной авторизации
- Возвращает ошибку 403 при неудаче

**Взаимодействие с БД:**
1. `UserModel.findById(childId)` - SQL-запрос для проверки принадлежности ребенка

**Уязвимости:**
1. **Потенциальная утечка информации**:
   Ошибки могут помочь злоумышленнику определить, существует ли ребенок.

2. **Нет проверки принадлежности ребенка к пользователю в контроллере**:
   Проверка делегирована middleware, но не реализована на уровне сервиса или модели.

### Общие уязвимости и рекомендации:

1. **Проверка email и пароля**:
    - Добавить валидацию формата email
    - Добавить проверку сложности пароля
    - Реализовать rate limiting для предотвращения брутфорса

2. **Проверка пин-кода**:
    - Добавить проверку минимальной длины пин-кода
    - Добавить проверку на уникальность пин-кода для ребенка

3. **Управление токенами**:
    - Реализовать защиту от replay-атак для refresh токенов
    - Добавить ограничение на количество активных refresh токенов на пользователя
    - Реализовать механизм отзыва всех токенов
    - Добавить автоматическую очистку истекших refresh токенов

4. **Безопасность аутентификации**:
    - Добавить проверку "свежести" токена
    - Добавить проверку соответствия токена refresh токену
    - Реализовать защиту от session fixation

5. **Авторизация**:
    - Добавить проверку принадлежности ребенка к пользователю на уровне сервиса
    - Реализовать более строгую проверку прав доступа

6. **Логирование**:
    - Добавить логирование неудачных попыток входа
    - Реализовать аудит действий пользователей

7. **Конфигурация безопасности**:
    - Добавить использование Helmet для настройки безопасности HTTP-заголовков
    - Реализовать защиту от XSS и CSRF
    - Настроить CORS с ограничениями

Эти рекомендации помогут усилить безопасность приложения и предотвратить потенциальные уязвимости.